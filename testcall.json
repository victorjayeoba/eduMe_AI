// 1. Create scenario
// ... your existing Node.js setup

var postData1 = JSON.stringify({
    name: "EduMeAi Test Scenario",
    description: "Basic TTS prompt",
    script: [
      { say: { text: "Hello from EduMeAi. This is a test call. Goodbye!" } },
      { hangup: {} }
    ]
  });
  
  req.write(postData1);
  req.end();
  // log scenarioKey from response.
  
  // 2. Launch call (after scenarioKey known)
  var options2 = {
    method: "POST",
    hostname: "69k5y8.api.infobip.com",
    path: "/voice/ivr/1/messages",
    headers: {
      Authorization: "{authorization}",
      "Content-Type": "application/json"
    }
  };
  
  var req2 = https.request(options2, function(res) { /* log */ });
  
  var postData2 = JSON.stringify({
    scenarioId: "<scenarioKey>",
    from: "<yourInfobipNumber>",
    destinations: [
      { to: "+2347026702294" }
    ]
  });
  
  req2.write(postData2);
  req2.end();
  